import { State, Callable } from './state';
declare type Effect = (this: State) => void | VoidFunction;
declare function createEffect(setEffects: (state: State, cb: Callable) => void): (callback: Effect, values?: unknown[] | undefined) => void;
export { createEffect };
